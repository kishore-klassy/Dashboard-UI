
module.exports = {
  darkMode: 'class', // Enables dark mode based on the 'class' strategy
  theme: {
    extend: {
      colors: {
        primary: '#2563EB', // Example blue color
        secondary: '#1E3A8A',  // Dark blue for secondary themed settings
      },
    },
  },
  plugins: [],
};
